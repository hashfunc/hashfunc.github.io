import type { NextPage } from "next";
import Head from "next/head";

const Home: NextPage = () => {
  return (
    <div>
      <Head>
        <title>hashfunc</title>
      </Head>
      <main>
        <p>hashfunc</p>
      </main>
    </div>
  );
};

export default Home;
